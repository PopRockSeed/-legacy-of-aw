services:
  web:
    image: node:22
    working_dir: /app
    volumes:
      - ./:/app
      - node_modules:/app/node_modules
    command: sh -c "npm install && npx vite"
    ports:
      - "3000:3000"

volumes:
  node_modules:
